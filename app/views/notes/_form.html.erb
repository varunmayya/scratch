<div class="logo">Scratch<span class="yellow">!</span></div>
<%= form_for(@note) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="white-box">
    <%= f.text_area :data, :class => "ip"%>
	<div class="actions">
		<div class="lhs">
			<%unless user_signed_in? %>
    You must be <%=link_to '<span class="sblue">signed in</span>'.html_safe, new_user_registration_path%> save notes.
   
    <% else %>
     <%=current_user.email%> 
    <%end%>
			</div>
	    <%= f.submit :Save, :class => "submit-right" %>
	  </div>
  </div>
  
<% end %>
